// La función fs.readdir() recibe como parámetros: una ruta(path) y un
// callback. La firma del callback es:
//function callback (error, lista) { /* ... */ }

var fs = require('fs');

//path nos dice que extencion tiene un archivo o la ruta del archivo
var path = require('fs');

//leyendo la ruta de los archivos. 